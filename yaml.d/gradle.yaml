apps:
  - name: gradle
    group: gradle group
    license: gradle license:
    url: https://gradle.org/
    summary: gradle summary
    description: Gradle is a build automation tool known for its flexibility to build software
    arches: x86_64
    download_page: PKGURL=$(eget --list --latest https://gradle.org/releases/ gradle-$VERSION-bin.zip)
    download_mask: gradle-$VERSION-bin.zip
