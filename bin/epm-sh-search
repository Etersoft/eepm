#!/bin/sh
#
# Copyright (C) 2012-2025  Etersoft
# Copyright (C) 2012-2025  Vitaly Lipatov <lav@etersoft.ru>
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program. If not, see <http://www.gnu.org/licenses/>.
#

# Common search functions for epm search and epm play --search

# Get the longest word from arguments (for searching via package manager)
# PM search works better with longer words
__epm_get_longest_word()
{
    local longest=""
    local word
    for word in "$@" ; do
        # skip exclusion patterns
        case "$word" in
            ~*) continue ;;
        esac
        # clean glob characters for PM search
        word=$(echo "$word" | sed -e "s/[?*\[\]]//g")
        [ -z "$word" ] && continue
        if [ ${#word} -gt ${#longest} ] ; then
            longest="$word"
        fi
    done
    echo "$longest"
}

# Convert glob pattern to regexp
# Supports: * ? ^ $ (|) [abc] [!abc] \[ \]
__epm_glob_to_regexp()
{
    local pattern="$1"
    # escape: . { } +
    # keep \ ^ $ ( ) | [ ] for extended patterns and escaping
    echo "$pattern" | sed \
        -e 's|[.{}+]|\\&|g' \
        -e 's|\*|.*|g' \
        -e 's|?|.|g' \
        -e 's|\[!|\[^|g'
}

# Build grep filter pipeline for search results
# Args: patterns (use ~pattern for exclusion)
# Uses: $short, $regexp, $EGREPCOLOR
__epm_build_grep_filter()
{
    local i
    [ -z "$*" ] && return

    local list=""
    local listN=""

    # separate include and exclude patterns
    for i in "$@" ; do
        case "$i" in
            ~*)
                listN="$listN ${i#\~}"
                ;;
            *)
                list="$list $i"
                ;;
        esac
    done

    list=$(echo "$list" | sed 's/^ *//' | sed 's/ *$//')
    listN=$(echo "$listN" | sed 's/^ *//' | sed 's/ *$//' | sed 's/ /|/g')

    # convert to regexp (unless --regexp mode)
    if [ -z "$regexp" ] ; then
        list=$(__epm_glob_to_regexp "$list")
        listN=$(__epm_glob_to_regexp "$listN")
    fi

    # short mode: strip description
    if [ -n "$short" ] ; then
        echon " | sed -e 's| .*||g'"
    fi

    # exclusion filter
    [ -n "$listN" ] && echon " | grep -E -i -v -- \"$listN\""

    # inclusion filters (one per word if multiple)
    local word_count
    word_count=$(echo "$list" | wc -w)
    if [ "$word_count" -gt 1 ] ; then
        for i in $list ; do
            echon " | grep -E -i -- \"$i\""
        done
    fi

    # colorize matches
    local COLO=""
    for i in $list $listN ; do
        [ -n "$COLO" ] && COLO="$COLO|"
        COLO="$COLO$i"
    done

    if [ -n "$list" ] ; then
        echon " | grep -E -i $EGREPCOLOR -- \"($COLO)\""
    fi
}

# Colorize search output: package names, search pattern, and epm play hint
# Input format: "package-name - description" or "package-name"
# Uses: __epm_color_pattern - regex pattern to highlight (set by __epm_search_make_grep)
# Package names: CYAN, search pattern: RED (bold), "epm play" hint: GREEN
__epm_colorize_search_output()
{
    local hint="(use 'epm play' to install it)"

    if [ -z "$USETTY" ] ; then
        # no color - just replace marker with plain hint
        sed "s| __EPM_PLAY_HINT__| $hint|"
        return
    fi

    local pattern="${__epm_color_pattern:-}"
    local colored_hint="$(printf '\033[32m')$hint$(printf '\033[0m')"

    # Use awk for reliable ANSI color handling
    awk -v cyan="$(printf '\033[36m')" -v green="$(printf '\033[32m')" \
        -v red="$(printf '\033[1;31m')" -v reset="$(printf '\033[0m')" \
        -v pattern="$pattern" -v colored_hint="$colored_hint" -v hint="$hint" '
    {
        line = $0

        # remove marker temporarily (will add colored hint at the end)
        has_hint = 0
        if (index(line, " __EPM_PLAY_HINT__") > 0) {
            has_hint = 1
            gsub(/ __EPM_PLAY_HINT__/, "", line)
        }

        if (match(line, /^[^ ]+ - /)) {
            # format: "package - description"
            sep = index(line, " - ")
            pkg = substr(line, 1, sep - 1)
            rest = substr(line, sep)

            # colorize pattern in package name (return to cyan after red)
            if (pattern != "") {
                IGNORECASE = 1
                gsub("(" pattern ")", red "&" cyan, pkg)
            }

            # colorize pattern in description (return to reset after red)
            if (pattern != "") {
                gsub("(" pattern ")", red "&" reset, rest)
            }

            output = cyan pkg reset rest
        } else if (match(line, /^[^ ]+$/)) {
            # format: "package" only
            pkg = line
            if (pattern != "") {
                IGNORECASE = 1
                gsub("(" pattern ")", red "&" cyan, pkg)
            }
            output = cyan pkg reset
        } else {
            # other format - just colorize pattern
            if (pattern != "") {
                IGNORECASE = 1
                gsub("(" pattern ")", red "&" reset, line)
            }
            output = line
        }

        # add colored hint at the very end
        if (has_hint) {
            output = output " " colored_hint
        }

        print output
    }'
}
